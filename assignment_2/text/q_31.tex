%&tex
We can tell that `setPrice' is a method rather than a constructor from
looking at its header because the header contains a type signature for
what `setPrice' will return, which in this case is void.
