%&tex
A method which attempts to call a \verb|return| statement with other
actions to follow will not compile correctly. In the case of compiling
with BlueJ, it will complain that all subsequent statements are
unreachable.
